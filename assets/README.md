# Demo images for the README

Place screenshots here so the README renders them on GitHub:

- `0820.png` — main hand / simulation screenshot
- `image.png` — secondary screenshot (optional)

If these files are missing locally, the Markdown image links will show as broken until you add the PNGs.
